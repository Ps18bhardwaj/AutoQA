[pytest]
asyncio_mode = auto
addopts = -ra -m "not browser and not llm"
testpaths = tests
markers =
    browser: needs an installed Chromium (playwright install chromium) — run with `pytest -m browser`
    llm: needs real API keys — run with `pytest -m llm`
